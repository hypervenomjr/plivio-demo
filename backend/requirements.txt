chromadb==0.5.5
sentence-transformers==3.0.1
faster-whisper==1.0.3
transformers==4.57.6
scipy==1.14.0
webrtcvad==2.0.10

# webrtcvad 2.0.10 imports pkg_resources at module load. setuptools 81
# deprecated it and 84 removed it, so a current setuptools breaks webrtcvad
# with ModuleNotFoundError: No module named 'pkg_resources'.
setuptools<81

# torch is deliberately NOT pinned: Colab ships a build matched to its CUDA
# runtime, and forcing a specific version either takes ten minutes to install
# or silently breaks GPU support for llama.cpp and faster-whisper.

# llama-cpp-python is deliberately NOT listed: it needs CUDA build flags or it
# installs a CPU-only build that runs at 1-3 tok/s with no error.
# See backend/colab_notebook.md.
